import React from 'react';
import AppWithDetails from './AppWithDetails';

const Router: React.FC = () => {
  // Semua routing sekarang ditangani oleh React Router di AppWithDetails
  return <AppWithDetails />;
};

export default Router;
